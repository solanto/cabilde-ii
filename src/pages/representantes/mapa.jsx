// rep map search page (map mode)
// eventually, embed GIS-based search applet here

import * as React from "react"
import Layout from "../../components/representantes/index-layout"

const Mapa = props => (
    <Layout tablaLabel="Cambia a la tabla" mapaLabel="Mapa" selected="mapa" />
)

export default Mapa
